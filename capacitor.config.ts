import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theninjacare.user',
  appName: 'theNinjaCare',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
